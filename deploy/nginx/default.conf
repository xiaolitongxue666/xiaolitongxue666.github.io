server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Astro base=/blog/ 仅影响 HTML URL；静态文件仍在 dist/ 根。
    # rewrite 将 /blog/* 映射到容器内静态根后再 try_files。
    location /blog/ {
        rewrite ^/blog/(.*)$ /$1 break;
        try_files $uri $uri/ /index.html;
    }
}
