---
import { SITE } from '../lib/site';
import { withBase } from '../lib/base';

interface Props {
  title?: string;
  description?: string;
  keywords?: string | string[];
  canonicalUrl?: string;
}

const {
  title,
  description = SITE.description,
  keywords,
  canonicalUrl,
} = Astro.props;

const pageTitle = title ? `${title}` : SITE.title;
const keywordContent = Array.isArray(keywords) ? keywords.join(',') : keywords ?? '';
const canonical = canonicalUrl ?? new URL(Astro.url.pathname, SITE.url).href;
---

<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="content-type" content="text/html;charset=utf-8" />
    {keywordContent && <meta name="keywords" content={keywordContent} />}
    <meta name="description" content={description} />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{pageTitle}</title>
    <meta name="site-base" content={import.meta.env.BASE_URL} />
    <link rel="stylesheet" href={withBase('/assets/css/default.css')} type="text/css" />
    <link rel="stylesheet" href={withBase('/assets/css/small.css')} type="text/css" media="(max-width: 720px)" />
    <link rel="stylesheet" href={withBase('/assets/css/syntax.css')} type="text/css" />
    <link rel="canonical" href={canonical} />
    <meta property="og:title" content={pageTitle} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonical} />
    <meta property="og:site_name" content={SITE.title} />
    <meta property="og:type" content="website" />
    <meta name="twitter:card" content="summary" />
    <meta name="twitter:title" content={pageTitle} />
    <meta name="twitter:description" content={description} />
    <link rel="shortcut icon" href={withBase('/assets/images/avatar.jpg')} type="image/jpeg" />
    <script src={withBase('/assets/js/theme-toggle.js')} is:inline></script>
    <script src={withBase('/assets/js/floating-buttons.js')} is:inline></script>
  </head>
  <body>
    <slot />
  </body>
</html>
