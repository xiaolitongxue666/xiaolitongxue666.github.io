---
description: xiaolitongxue666 Astro 博客项目约定、约束与已知陷阱
alwaysApply: true
---

# 博客项目 Agent 规则

Astro 站点 `xiaolitongxue666.github.io`。`master` 存源码；Pages Source = **GitHub Actions**。

## 不可破坏

- 勿重命名/删除已有 `_posts/`（permalink 由文件名 slug 决定，含历史大小写混用）
- 勿改 permalink（`/:year/:month/:day/:title/`）
- Obsidian 契约：`_posts/` + `assets/images/posts/` + front matter `layout: default`
- 首页 build-version：合并前提交 `_data/build.yml`
- 根 `package.json` 勿加 `"type": "module"`
- overrides 只钉 `gray-matter` 的 js-yaml 3.x；见 [docs/DEPENDENCIES.md](../../docs/DEPENDENCIES.md)

## 常见陷阱

- **build-version date**：js-yaml 可能把 `date` 解析成 Date，须 `formatDateValue()`
- **分页**：`/page2/` 不是 `/page/2/`；`POSTS_PER_PAGE = 10` 与 `floating-buttons.js` 一致
- **静态资源**：`public/assets` → 根 `assets/`；子路径链接走 `withBase()`
- **VPS**：`ASTRO_BASE=/blog/` 仅 VPS 构建；公网入口 `https://<域名>/blog/`；`curl --noproxy '*'`
- **部署**：只认 `deploy-vps.yml`（compose `blog`+`goatcounter`，探针成功后再切 `current`）。`edge` 仅 `profiles: [local]`。勿手推 `current`。
- **E2E**：`E2E_OUT_DIR=dist-e2e`；staging 须 `npm ci`；勿把 fixture 提交到 `_posts/`
- **Shiki**：`data-theme`；须有 `[data-theme="dark"] .shiki`
- **统计**：上报勿 `withBase()`；联调 `npm run local:vps`，见 [blog-analytics.md](../../memory_skills/blog-analytics.md)

## 文档

- [AGENTS.md](../../AGENTS.md)
- [memory_skills/README.md](../../memory_skills/README.md)
- [docs/ARCHITECTURE.md](../../docs/ARCHITECTURE.md)、[docs/TROUBLESHOOTING.md](../../docs/TROUBLESHOOTING.md)

## 验证

```bash
npm run verify:local
```

`run-ci-parity.sh` 是 CI 子集，不是 `verify:local` 的替代。
