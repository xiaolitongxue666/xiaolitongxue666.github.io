---
description: xiaolitongxue666 Astro 博客项目约定、约束与已知陷阱
alwaysApply: true
---

# 博客项目 Agent 规则

## 仓库角色

Astro 站点 `xiaolitongxue666.github.io`。`master` 存源码；GitHub Pages **Source = GitHub Actions**（非内置 Jekyll）。

## 不可破坏

- 勿重命名/删除已有 `_posts/`（permalink 由文件名 slug 决定，含历史大小写混用）
- 勿改 permalink 规则（`/:year/:month/:day/:title/`）
- Obsidian 契约：`_posts/` + `assets/images/posts/` + front matter `layout: default`
- 首页 build-version：commit + `YYYY-MM-DD` date；合并前提交 `_data/build.yml`

## 常见陷阱

- **package.json**：勿加 `"type": "module"`（E2E 脚本为 CommonJS）
- **build-version date**：`_data/build.yml` 的 date 经 js-yaml 可能变 Date，须 `formatDateValue()`
- **分页 URL**：`/page2/` 不是 `/page/2/`（`src/pages/[page].astro` + param `page2`）
- **静态资源**：`public/assets` 符号链接到根 `assets/`；子路径部署须 `withBase()`（见 `src/lib/base.ts`）
- **VPS 子路径**：`deploy-vps.yml` + `vps_nginx` **hybrid**（公网 `https://<域名>/blog/`）；勿在 Pages 构建中设 `ASTRO_BASE`
- **VPS curl**：验收须 `curl --noproxy '*'`
- **E2E**：`E2E_OUT_DIR=dist-e2e`；staging 须 `npm ci`（Node >= 22.12）；勿提交 E2E fixture 到 `_posts/`
- **POSTS_PER_PAGE**：`src/lib/pagination.ts` 与 `floating-buttons.js` 均为 10

## 文档优先

- [memory_skills/README.md](../memory_skills/README.md)（VPS / 子路径）
- `docs/ARCHITECTURE.md`、`docs/TROUBLESHOOTING.md`
- `docs/PROJECT_MEMORY.md`、`AGENTS.md`

## 验证

```bash
bash .github/scripts/e2e/run-ci-parity.sh
```
